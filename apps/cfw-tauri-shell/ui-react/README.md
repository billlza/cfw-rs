# CANCELLED — not a product UI path

React is a frontend library. **Tauri** is the native shell. Replacing the WebKit UI with React does **not** make the app stronger than Tauri, and is not a 0.3.1 goal.

Product UI: [`../ui`](../ui) (Tauri `frontendDist`).

This folder is retained only so old links do not 404; it is not built in CI and must not be advertised as the migration target.
