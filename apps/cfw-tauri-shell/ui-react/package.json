{
  "name": "cfw-tauri-shell-ui-react",
  "private": true,
  "version": "0.3.1",
  "scripts": {
    "build": "esbuild src/main.jsx --bundle --outfile=dist/app.js --format=esm --jsx=automatic --target=safari16 && cp index.html dist/index.html && cp src/styles.css dist/styles.css",
    "dev": "npm run build"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
