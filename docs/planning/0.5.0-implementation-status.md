# 0.5.0 implementation status — 2026-09-22

## User correction: preserve the 0.4 UI and interaction model

The user rejected the Overview redesign and installed UI preview. The accepted
scope is **the same 0.4 pages, layout details, functionality and interaction paths,
with suitable native SwiftUI/Liquid Glass component treatment**. The earlier
experiment is not an accepted 0.5 UI milestone; test counts below establish only
engineering behavior, not design acceptance or layout parity.

The [UI fidelity contract](0.5.0-ui-fidelity-contract.md) supersedes the earlier
interface plan. The frozen page inventory, General renderer, CSS, HTML and
window configuration remain unchanged. Native component adapters extend the
original app handlers rather than replacing the pages. Full component integration
and installed acceptance remain unfinished; shared Rust/ABI work is reused
without retaining the rejected presentation.

The user authorized parallel 0.4.0 remediation and 0.5.0 implementation. The initial 0.4.0
recipient report has arrived: installation identity/signature checks passed, the
CFM launchd jobs were not found and the registration-status CLI hit the Host
operation lease. A targeted follow-up is pending to identify that lease holder
and the original Unavailable trigger; publication and runtime acceptance remain open. The frozen 40073 source, signed app, canonical
packages, original journals and separate repair worktree are preserved. The
cancelled recurring monitor has not been recreated.

## Engineering experiments retained; visual direction rejected

- An actual SwiftUI/AppKit overview, built and linked through an opt-in development
  feature in the existing host, with a native menu entry and shortcut.
- Observations of the single existing Rust coordinator; separate core, System
  Proxy and TUN states; unknown/failure and non-ready states remain explicit.
- Actual core/System Proxy/TUN controls share the extracted Rust use cases with
  the existing Tauri commands, including their queue, snapshot-sensitive enables,
  owned stops, retirement/profile/capability/authorization and maintenance rules.
- Matching pending/result feedback, bounded duplicate/replay/stale refusal,
  command ownership after window close, and explicit same-mode Retry/Continue
  Approval. An explicit retry still awaiting approval uses the existing system
  settings navigation; no automatic approval or permission change is introduced.
- Versioned C ABI with bounded frames, main-thread ownership, one pending delivery,
  session isolation, cancellation and exactly-once close callbacks.
- English, Simplified/Traditional Chinese and Japanese resources; native glass
  toolbar control on supported systems and standard accessibility/OS fallback.
- The four relevant Rust files from the separately tested 0.4 startup/read/quit
  repair, so the internal snapshot read cannot retrigger failed reconciliation.

The host currently still uses Tauri for application composition and the existing
control surface. This is not the completed standalone SwiftUI replacement, full
90-command parity, a newly signed build, or an installed VPN validation.

## Evidence

Private run artifacts: `target/swiftui-050-20260922/`. Keep the initial failed
compiles/localization checks, missing UI build failure, and the deterministic
window-reopen sequence regression alongside final results. The latter reproduced
`present(session1, sequence1) → publish(session1, sequence10) → present(session2,
sequence2)` rejecting the new window. Session ordering is now independent of the
sequence within a session; old frames still cannot overwrite a new observer.

The earlier read-only slice passed 97 application tests, 408 host tests, 156 existing UI tests,
9 Swift tests, the actual Rust/Swift/AppKit FFI executable, four source-binding
negative tests, source-contract verification, Rust formatting and Clippy with
warnings denied. A separate actual-window assertion confirms the SwiftUI toolbar
is installed. The shared library was built with Swift warnings treated as errors.
No 0.5.0 hosted CI, signed candidate or installed-network acceptance is claimed.

`native/dashboard/README.md` documents the verified commands, explicit fixture
scope, resource ownership and remaining integration work. Render fixtures are
visual checks only, not observations of the installed CFM network. Debug timing
samples cover Swift decode/model acceptance only; the roadmap's full product
performance targets remain unproven.

## Dependency and VPN review

Official checks on 2026-09-22 still identify [Rust 1.98.1](https://blog.rust-lang.org/2026/09/03/Rust-1.98.1/)
and [sing-box 1.14.1](https://github.com/SagerNet/sing-box/releases/tag/v1.14.1)
as current stable releases, matching the existing pins. No compiler, engine,
dependency lock or engine patch has been updated in this UI change.

The available [saphyr-parser 0.1.0](https://crates.io/crates/saphyr-parser/0.1.0)
changes plain empty-node events from `"~"` to `""` ([upstream change](https://github.com/saphyr-rs/saphyr/pull/129)).
The current null-value path already handles both, but mapping keys retain scalar
text and need an explicit compatibility decision. Upgrade separately, retaining
alias/anchor/tag/multidocument rejection and validating exact depth/event limits.
This check covers these three versions, not the entire transitive dependency graph.

Current schema/import and Apple build support do not establish OpenVPN/OpenConnect
support merely because sing-box provides those endpoints. Their schemas,
credentials, interactive authentication, backend build, DNS/routes and real-server
interoperability need separate implementation and packet evidence. Preserve current
WireGuard, UDP/QUIC and IPv6 behavior through UI migration first.

## Current control validation

Private run artifacts: `target/swiftui-controls-050-20260922/`. The native control
boundary was independently reviewed against the old dashboard. The same-mode
retry regression was reproduced before the fix: both AwaitingApproval and Failed
rejected the same enabled=true intent. Its passing regression now requires exactly
one admitted callback without first changing the switch to Off. A separate red/green
projection case proves a non-ready runtime no longer appears connected.

The real Rust/Swift/AppKit FFI executable invokes native control admission and
receives typed Rust callbacks; it covers stale window/request rejection, pending
work, failed completion, explicit approval retry and exactly-once window release.
Its callback is a fixture and never changes the installed VPN. Unit checks also
cover explicit-retry-only settings navigation; the actual system pane and live
signed-service authorization remain unverified by this development run.

Final checks passed: 97 application tests, 417 host tests with native controls,
407 host tests with that feature disabled, 156 existing UI tests, 14 Swift tests,
the actual Rust/Swift/AppKit FFI executable, four source-binding negative tests,
source-contract verification, Rust/Swift format checks and Clippy with warnings
denied. The private verification receipt records the actual commands, log hashes,
retained failures and scope. Four-language light/dark failed
and approval views were rendered at the minimum window size (16 fixtures); Chinese
light and Japanese dark approval views were visually inspected. Debug decode/model
samples remain a component baseline, not product performance improvement evidence.

## Next integration boundary

Use 0.4 as the fixed visual/interaction reference. Inventory each page and
significant menu/dialog state, then replace suitable existing component families
in place while retaining real command/data bindings. Preserve all unmigrated
working controls. Compare the whole page and action sequence before further
migration. Do not continue expanding the rejected Overview or replace the default
UI until visual, interaction, accessibility and runtime parity are demonstrated.

## Shared diagnostic repair after recipient report

The exact read-only registration Status action no longer acquires the Host
operation lease. It returns no engine-Off proof and cannot register/unregister
services. All ten other maintenance actions and the runtime query retain their
existing lease checks. The target regression failed before correction, four focused
checks and 141 NativeBridge tests passed in the 0.4 repair worktree. The native
package, fixtures and contracts (301 files) match this worktree byte-for-byte.
This shared correction is c0f0c81 here (90054de in the 0.4 repair branch). It has
not been installed or verified on the recipient's Mac and does not establish the
root cause of the initial Unavailable. Frozen build 40073 remains unchanged.

## Historical UI preview installation — rejected design

A separate `Clash for Mac 0.5 Preview.app` was installed for local inspection. It
loads the same native presentation library and packaged resources, with an
independent identity, explicit sample-state labels and disabled network controls.
Its menus exercise example states, languages and appearance. It is not the full
0.5 VPN client: the production host still shares settings, Keychain lineage and
service identity with 0.4, so simply renaming that host would not isolate it.

The preview has no production network bridge, coordinator, updater, service
components or entitlement set. It uses an ad hoc local signature, not Developer
ID notarization. Fourteen Swift tests and the packaged 20-frame AppKit self-check
passed; actual installed-window inspection confirmed the toolbar, persistent
sample notice and disabled switches. Frozen 40073 was preserved. The isolated
launcher and reproducible builder are under `native/dashboard/Preview/`.


## In-place components checkpoint — 2026-09-22

The user explicitly requested completing the corrected 0.5.0, installing a usable
preview, and removing the rejected preview. The goal is still active. The rejected
`com.bill.clashformac.ui-preview` app was moved from `~/Applications` to
`~/.Trash/Clash for Mac 0.5 Preview-rejected-20260922-104441.app`; its installed
path is absent. Receipt: `target/050-completion/rejected-preview-removal.json`.
Original 40073 and historical failure evidence remain intact.

The opt-in `native-ui` feature now connects two original surfaces to SwiftUI/AppKit:

- Profiles context menu: the same twelve actions, selected-profile omission,
  source metadata and engine-Off disabled reasons return through the unchanged
  `runProfileMenuAction`. Native geometry preserves the original content-viewport
  positioning; the effective page theme, Escape/outside-click dismissal, scrolling
  and Command-Q passthrough are explicit. Cancelled/obsolete presentation messages
  cannot execute late actions.
- Network settings dialog: the original Port/MTU/Allow LAN entry points, field
  order, two checkboxes, Cancel/Apply, application validation, stored revision and
  error draft survive. Swift owns only the unsaved form. A submitted draft returns
  to `preferencesFromRuntimeDraft` and the original `save` function; opening or
  editing a form does not mutate networking. Saving blocks dismissal. Failures and
  touched-vs-inherited IPv6 choices are retained. Hiding the parent retains edits. Every submission has a distinct response ID;
  repeated invalid submissions and unrelated theme/error frames cannot strand
  the form in pending state or acknowledge a different submission. Both failure
  conditions were reproduced before correction and retained as regressions.

Six new commands own presentation only, alongside the original 90 business
commands. No second network coordinator or altered General layout is introduced.
The old Overview requires its separate `native-dashboard` feature. The `native-ui`
feature remains development-only; release packaging still refuses it until its
signed composition is admitted.

Checks at this checkpoint: 413 native-feature host tests, 410 feature-disabled
host tests, 169 UI tests, 37 Swift tests including real AppKit windows, Rust Clippy
with warnings denied, Swift strict lint, source-contract verification and four
source-binding tests. Raw logs, failures and removal receipt are in
`target/050-completion/`. AppKit tests establish behavior and bounded geometry,
not complete visual/accessibility acceptance. AX inspection in the isolated test
process yielded no SwiftUI child tree; cacheDisplay produced an empty material
surface and ImageRenderer cannot render the platform input controls. Retain these
failed observations; do not count them as screenshots or VoiceOver acceptance.

Remaining before handing over the requested usable preview: actual WKWebView /
native window integration and visual/keyboard/accessibility parity, remaining
appropriate in-place component treatments, signed bundle/version composition,
stable dependency compatibility, product performance and installed network checks.
No new 0.5 application has been installed or advertised as complete.

## Recipient v2 evidence

The new report collected at 2026-09-22T03:19:52Z identifies the 40073 GUI itself,
PID 9775, as the Host lease UDP49374 owner at all three observations. The frozen
status CLI takes that same lease before reading registration status, explaining
its Busy/70 failure. `90054de` in the repair branch (c0f0c81 here) addresses that
specific read-only diagnostic failure. Launch override `enabled` and absent
launchd jobs do not supply the missing SMAppService status or original Unavailable
cause. Clash Party coexistence is observed, but it is not the observed lease
competitor. Startup journals / a bounded stack sample remain the next discriminator;
no recipient repair or successful VPN session is claimed.

## Preview packaging and live component checkpoint — 2026-09-22

The corrected product now has an explicit **0.5.0 / 50001** preview identity,
separate from the frozen 0.4.0 / 40073 GA identity. Eleven local Cargo packages,
the lockfile, core version, Tauri configuration and generated Xcode project match.
The default release version checker still rejects preview inputs; the 0.5 CI
version step explicitly chooses `--preview`. The historical GA build/signing
transaction is unchanged. The older unsigned-CI candidate builder still needs a
separate 0.5 validation identity before claiming hosted packaging CI readiness.

`prepare_preview_candidate.sh` composes the real existing Host, all original
network components, and the native component library/resources into an immutable
pre-sign input. It requires clean source, exact verified tools and dependencies,
and refuses an already allocated preview. It does not install or launch an app.
The release build rejects the abandoned `native-dashboard` feature and consumes
verified prebuilt SwiftUI artifacts only, with package-relative runtime paths.
The shared bundle/signature verifiers now understand explicit preview contexts;
GA defaults stay frozen. Runtime handoff admission retains canonical installation,
Developer ID, notarization, ownership and network safeguards, checks the exact
preview version/build, and verifies the new UI dylib as another signed component.

The actual Release Swift library was compiled with warnings denied, inspected as
thin arm64/macOS 15, and copied to an isolated verification directory. Its Swift
compiler compatibility rpath was identified and removed before any signing;
non-system/non-package runtime dependencies are rejected. This establishes a
portable library, not a signed application. Signed manifest promotion retains
its pre-sign artifact/manifest binding and must not modify localization resources.

A separate **CFM Component Integration Test — TEST DATA** app loads the real
built frontend in WKWebView and the actual Swift C ABI. Native menu Edit returned
to the unchanged HTML editor. The native form preserved repeated save failures,
rejected empty trusted LAN ranges through the original validator, and after a
valid fixture save the unchanged General page displayed port 8990. These writes
were confined to fixture memory. No production coordinator, Keychain or network
was used by that harness.

Background automation clicks after native windows closed did not reach WebKit.
Passive AppKit logs showed the failed clicks while the test application was
inactive with no key/main window. A test activation request alone did not prove
activation. Opening that **same running application** through Finder restored
normal editor-close and navigation behavior, without restarting its process or
changing product code. Preserve this automation/activation limitation; neither
claim a normal-user focus defect nor count it as complete installed lifecycle
acceptance. The test app was closed after inspection.

Verified at this checkpoint: 413 native-feature Host tests, Clippy with warnings
denied, 136 focused packaging/identity/manifest regressions, eight native release
observation tests, generated Xcode equivalence, static source contract and native
product graph. Actual compiler and source hashes, retained failures, AppKit logs
and the original dependency mismatch are in `target/050-completion/`.
The old generated esbuild wrapper was retained; the one differing dependency
file was restored from the already verified release dependency tree and that
whole tree rechecked. Existing source-built Libbox was copied and reverified;
40073 itself was not rebuilt, resigned or replaced.

The complete preview application has not yet been signed, notarized or installed.
Full production Host integration, remaining in-place UI treatment, dependency
compatibility, product performance and installed VPN acceptance remain open.

The first real pre-sign attempt, **50001**, is retained under
`target/candidates/0.5.0/preview-preflight/50001`. All four native network products
and the tombstone built, then SwiftUI preparation rejected the successful Swift
version query because its driver banner was on stderr. This was a verifier bug,
not a product compilation failure. The driver query now accepts only that exact
version-banner form and still rejects warnings, wrong targets and nonzero exits;
its regression and live pinned-compiler query pass. Current preview identity is
advanced to **50002** so the corrected source cannot overwrite or masquerade as
50001. Historical 0.4 GA identity remains 40073. No 50001 app was signed or installed.

## Stable YAML dependency compatibility — 2026-09-22

A fresh official crates.io inventory covered all 22 direct workspace registry
dependencies. Only saphyr-parser had a newer stable release; it is now locked to
0.1.0 with the published crate checksum. The lockfile changes only that package.
Rust 1.98.1 and sing-box 1.14.1 remain the verified current stable pins. This is
not a claim that every transitive package can be independently upgraded.

The old 0.0.12 parser passed 13 fixed compatibility assertions. On 0.1.0, three
failed for implicit empty-node text and mapping-key identity. The loader now
restores the old `"~"` text only for untagged plain implicit empties. Quoted and
explicitly tagged empty strings retain their text. Anchor/alias/tag, duplicate
key, multiple-document, 16/17-depth and actual 200000/200001-event boundaries
remain enforced. Direct rule-payload and routing-rule consumers have regressions.
All 420 native-feature Host tests pass with the new dependency. Evidence is in
`target/050-completion/yaml-dependency-upgrade/`; the old/new failures are retained.

Fresh RustSec and cargo-deny online checks encountered GitHub/crates.io transport
failures; these are not passing advisory or current-yanked checks. The package
build may proceed independently, but successful online policy verification remains
required before calling the preview complete. No policy or TLS check was disabled.

## Host build boundary correction — preview 50003

50002 built all seven native artifacts and the web UI, then its first real Host
release build exposed an inherited Cargo environment name containing `:`.
The native UI verifier's Bash seal could not unexport that name. The failed
preflight and full Host log remain under `preview-preflight/50002`; no app was
signed, notarized or installed. The verifier command now starts with an empty
environment and forwards only its explicit release inputs, retaining rejection
of unsigned-validation compiler selections. Cargo/plugin metadata and loader/shell
injection variables do not cross this boundary. A focused regression verifies
the command inputs; the next real Host build must prove the end-to-end fix.
Current preview identity is 50003; 50001 and 50002 remain rejected old identities.

## Release ABI and current dependency-policy proof — preview 50004

The real 50003 Host passed the corrected environment boundary, then failed at
final link: Swift's Release library did not export the seven component C entrypoints.
The two component entrypoint functions and callback aliases now have explicit
public visibility. The rejected Overview is still excluded from the release Host.
The library verifier checks the complete component C export set against the
header contract before Host compilation. The retained 50003 library fails this
new check; the new real Release library exports all seven symbols. A C program
using the actual headers linked and invoked all seven with invalid-input or
absent-window requests successfully, using an ad-hoc-signed private test copy.
Its earlier unsigned-loader refusal is retained separately. These are component
link/runtime checks, not installed product or network acceptance. All 37 Swift
component tests also pass. Current candidate identity is 50004, preserving 50003.

The latest RustSec database was obtained over the existing loopback proxy using
HTTP/1.1, with TLS verification retained. Its clean Git commit
`17af77682cecd2afa72b217ad7c6c30585d5003f` matches the current GitHub API response.
The locked macOS target graph's 331 packages passed RustSec and cargo-deny
advisories/current-yanked/bans/licenses/sources checks with zero warnings, notes
or errors. This retained local execution uses the same target graph, deny.toml,
commands and strict result validators, with an explicit transport environment;
it does not reclassify the earlier download failures or claim hosted CI success.
Receipts and raw outputs: `target/050-completion/yaml-dependency-upgrade/current-policy/`.

## Installed 50004 evidence and 50005 keyboard correction

The real 0.5.0/50004 Host was Developer ID signed, notarized, stapled, Gatekeeper
accepted and installed over the canonical application with verified 40073 backups.
Its separately notarized drag-and-drop DMG was mounted read-only; the contained
App matched the installed candidate's tree. Neither package is public GA.
Original signing-proof and resource-log compatibility failures remain retained
alongside explicit recovery receipts; no candidate app was silently rebuilt.

Installed observations confirm the original nine pages, original port-to-settings
entry, native 12-item profile menu and original JSON edit flow. Cancel, repeated
invalid port refusal and Command+Q followed by a normal relaunch worked locally.
These observations do not establish recipient repair, VPN acceptance or improved
product performance. The read-only maintenance status completed locally; the
recipient's initial Unavailable still lacks a confirmed cause.

50004 also exposed a real interaction regression: Escape immediately after
opening native settings did nothing until a text field received focus. The old
page listened for Escape at document level; SwiftUI onExitCommand only handled
focused content. The Cancel buttons now use the system cancelAction keyboard
shortcut in both glass and standard styles, preserving the existing busy guard.
A new real-AppKit key-equivalent test failed before the fix and passed after it;
all 38 component tests passed. A separate actual WKWebView/AppKit window confirmed
opening then immediately pressing Escape closes the dialog without focusing a
field. This harness uses test data and is not installed-product acceptance.

The next immutable preview identity is 50005. Preserve the signed 50004 App/DMG
and its failed Escape observation; do not overwrite or re-sign them. New installed
verification, broader UI/accessibility checks, performance and real VPN acceptance
remain required. No whole-product improvement or completed 0.5.0 claim is made.

50005 validation also ran Clippy across all Host targets with warnings denied.
It exposed a pre-existing test-only YAML budget fixture using map(...).count(),
which can omit its validation closure. The helper now folds through and validates
each parser event before counting; all 13 YAML tests, including the 200000/200001
event boundary, pass. No product YAML parsing or resource limit changed.
The 135 packaging/identity tests, eight migration-admission tests, eight release
observation tests, generated Xcode project comparison, strict Swift lint and Rust
format/source-contract checks passed. Clippy's initial failure is retained.

## 50005 installed startup race and 50006 correction

50005's App and DMG passed notarization, signatures, Gatekeeper and mounted-byte
verification. The App was installed with a verified 50004 rollback copy. The
staple operator initially created its new ticket with mode 0600; an independent
recovery copy changed only that ticket's mode to 0644 and passed all original
checks. The rejected copy and original failure remain retained.

Installed Escape without field focus and Command+Q from an open native panel
passed. A subsequent normal launch exposed another defect: General retained
`native startup reconciliation is pending` after startup-ready journal markers.
Reloading only the UI in the same process immediately recovered the actual Off
state, without network changes. The first engine snapshot precedes the event
subscriptions; reconciliation can complete in that interval, and Off does not
emit periodic replacement events. Journal-ready markers alone do not prove
coordinator reconciliation or record the exact event delivery time.

The 50006 source reads current status and runtime projection once after the
engine listeners are installed, then starts the existing controller load. It
does not retry reconciliation, mutate network state or add polling. A real
app.js bootstrap harness deterministically reproduced four failing schedules
on the old source. All 175 UI tests now pass, including completed Off, actual
Failed, rejected listener registration, later events and late stale replies.
The new installed candidate still must be built, signed and verified.

The retained 50005 package remains runtime-unaccepted. CFW monitoring recorded
2 SSL connect timeouts in 196 probes during the earlier wait, then 70/70 success
during actual installation and first launch, with unchanged system proxy. There
is a gap between those two monitoring intervals; neither proves zero interruption
or successful CFM VPN operation. Performance and full network acceptance remain
open. Evidence is under `target/050-completion/startup-listener-gap/`,
`installed-50005-startup-pending/` and `installed-ui-50005-observations.json`.

A bounded installed 50005 local-core smoke then passed real HTTP CONNECT and
SOCKS5h requests with TLS validation, using CFM's 127.0.0.1:7891 listener while
the existing CFW TUN/System Proxy stayed active. Normal Stop core released the
listener and returned all CFM mode controls to their initial Off state; system
proxy readback was unchanged. This used the already-running ProxyAgent process,
not a fresh 50005 service launch, and proves neither CFM TUN takeover nor direct
path performance. Receipt: `target/050-completion/local-core-smoke-50005/completed.json`.
