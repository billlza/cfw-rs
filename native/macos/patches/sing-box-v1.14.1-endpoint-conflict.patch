diff --git a/adapter/endpoint_conflict.go b/adapter/endpoint_conflict.go
new file mode 100644
index 0000000000000000000000000000000000000000..af7ffce8767178b4a9eff5b3c17a24750be47427
--- /dev/null
+++ b/adapter/endpoint_conflict.go
@@ -0,0 +1,54 @@
+package adapter
+
+import "errors"
+
+type EndpointConflictRole int32
+
+const (
+	EndpointConflictNone EndpointConflictRole = iota
+	EndpointConflictMixed
+	EndpointConflictController
+)
+
+type EndpointConflictError struct {
+	role  EndpointConflictRole
+	port  uint16
+	cause error
+}
+
+func (e *EndpointConflictError) Error() string {
+	if e == nil || e.cause == nil {
+		return "endpoint conflict has no cause"
+	}
+	return e.cause.Error()
+}
+
+func (e *EndpointConflictError) Unwrap() error {
+	if e == nil {
+		return nil
+	}
+	return e.cause
+}
+
+func NewEndpointConflictError(role EndpointConflictRole, port uint16, err error) error {
+	if err == nil {
+		return errors.New("endpoint conflict cause is required")
+	}
+	if port == 0 {
+		return errors.New("endpoint conflict port is required")
+	}
+	switch role {
+	case EndpointConflictMixed, EndpointConflictController:
+	default:
+		return errors.New("endpoint conflict role is invalid")
+	}
+	return &EndpointConflictError{role: role, port: port, cause: err}
+}
+
+func EndpointConflictFromError(err error) (EndpointConflictRole, uint16, bool) {
+	var conflict *EndpointConflictError
+	if errors.As(err, &conflict) && conflict != nil && conflict.cause != nil {
+		return conflict.role, conflict.port, true
+	}
+	return EndpointConflictNone, 0, false
+}
diff --git a/adapter/endpoint_conflict_test.go b/adapter/endpoint_conflict_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..8685643aceb8cea919b3eeccd6910868284d5c49
--- /dev/null
+++ b/adapter/endpoint_conflict_test.go
@@ -0,0 +1,39 @@
+package adapter
+
+import (
+	"errors"
+	"fmt"
+	"syscall"
+	"testing"
+)
+
+func TestEndpointConflictRoleSurvivesWrapping(t *testing.T) {
+	err := fmt.Errorf("start mixed inbound: %w", NewEndpointConflictError(EndpointConflictMixed, 7891, syscall.EADDRINUSE))
+	role, port, ok := EndpointConflictFromError(err)
+	if !ok || role != EndpointConflictMixed || port != 7891 {
+		t.Fatalf("unexpected conflict: role=%v port=%d ok=%v", role, port, ok)
+	}
+	if !errors.Is(err, syscall.EADDRINUSE) {
+		t.Fatal("wrapped conflict lost EADDRINUSE")
+	}
+}
+
+func TestUnclassifiedAddressInUseIsNotAConflict(t *testing.T) {
+	role, port, ok := EndpointConflictFromError(syscall.EADDRINUSE)
+	if ok || role != EndpointConflictNone || port != 0 {
+		t.Fatalf("bare errno was classified as a product endpoint: role=%v port=%d ok=%v", role, port, ok)
+	}
+}
+
+func TestInvalidEndpointConflictsAreNotClassified(t *testing.T) {
+	for _, err := range []error{
+		NewEndpointConflictError(EndpointConflictMixed, 7890, nil),
+		NewEndpointConflictError(EndpointConflictNone, 7890, syscall.EADDRINUSE),
+		NewEndpointConflictError(EndpointConflictMixed, 0, syscall.EADDRINUSE),
+	} {
+		role, port, ok := EndpointConflictFromError(err)
+		if ok || role != EndpointConflictNone || port != 0 {
+			t.Fatalf("invalid conflict was classified: role=%v port=%d ok=%v", role, port, ok)
+		}
+	}
+}
diff --git a/box.go b/box.go
index 7bbedd59aaf70bb84bfe835fceb8c1382231b349..f3c69d0a14cbe416230251277c06a4563401b944 100644
--- a/box.go
+++ b/box.go
@@ -509,7 +509,7 @@ func (s *Box) PreStart() error {
 }
 
 func (s *Box) Start() error {
-	err := s.start()
+	err := s.StartWithExternalCleanup()
 	if err != nil {
 		// TODO: remove catch error
 		defer func() {
@@ -523,6 +523,15 @@ func (s *Box) Start() error {
 		s.Close()
 		return err
 	}
+	return nil
+}
+
+// StartWithExternalCleanup leaves failed-start cleanup to the lifecycle owner.
+func (s *Box) StartWithExternalCleanup() error {
+	err := s.start()
+	if err != nil {
+		return err
+	}
 	s.logger.Info("sing-box started (", F.Seconds(time.Since(s.createdAt).Seconds()), "s)")
 	return nil
 }
diff --git a/daemon/instance.go b/daemon/instance.go
index 5e7502ce593de82bd1eefa23d4efe88bbb49ee4b..09b2713a97f19e9573ac252f75664ca1a32b175d 100644
--- a/daemon/instance.go
+++ b/daemon/instance.go
@@ -159,7 +159,7 @@ func attachInstance(ctx context.Context) *Instance {
 }
 
 func (i *Instance) Start() error {
-	return i.instance.Start()
+	return i.instance.StartWithExternalCleanup()
 }
 
 func (i *Instance) Close() error {
diff --git a/experimental/clashapi/endpoint_conflict_test.go b/experimental/clashapi/endpoint_conflict_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..fc4e119b199fd8c12f5dd1c5b13a758bf89d66d6
--- /dev/null
+++ b/experimental/clashapi/endpoint_conflict_test.go
@@ -0,0 +1,37 @@
+package clashapi
+
+import (
+	"errors"
+	"fmt"
+	"syscall"
+	"testing"
+
+	"github.com/sagernet/sing-box/adapter"
+)
+
+func TestClassifyControllerListenError(t *testing.T) {
+	cause := fmt.Errorf("listen failed: %w", syscall.EADDRINUSE)
+	err := classifyControllerListenError("127.0.0.1:9092", cause)
+	role, port, ok := adapter.EndpointConflictFromError(err)
+	if !ok || role != adapter.EndpointConflictController || port != 9092 || !errors.Is(err, cause) {
+		t.Fatalf("unexpected controller conflict: role=%v port=%d ok=%v err=%v", role, port, ok, err)
+	}
+}
+
+func TestClassifyControllerListenErrorRejectsOtherAndMalformedErrors(t *testing.T) {
+	ordinary := errors.New("permission denied")
+	for _, test := range []struct {
+		address string
+		err     error
+	}{
+		{"127.0.0.1:9092", ordinary},
+		{"127.0.0.1", syscall.EADDRINUSE},
+		{"127.0.0.1:0", syscall.EADDRINUSE},
+		{"127.0.0.1:65536", syscall.EADDRINUSE},
+	} {
+		classified := classifyControllerListenError(test.address, test.err)
+		if classified != test.err {
+			t.Fatalf("unexpected classification for %q: %v", test.address, classified)
+		}
+	}
+}
diff --git a/experimental/clashapi/server.go b/experimental/clashapi/server.go
index b9222ad54403ec051e005c1f303e48c5cefe0c15..c8d2d023c290be14377a5ad2ecca56dabedd6822 100644
--- a/experimental/clashapi/server.go
+++ b/experimental/clashapi/server.go
@@ -8,6 +8,7 @@ import (
 	"net/http"
 	"os"
 	"runtime"
+	"strconv"
 	"strings"
 	"syscall"
 	"time"
@@ -166,6 +167,7 @@ func (s *Server) Start(stage adapter.StartStage) error {
 			break
 		}
 		if err != nil {
+			err = classifyControllerListenError(s.httpServer.Addr, err)
 			return E.Cause(err, "external controller listen error")
 		}
 		s.logger.Info("restful api listening at ", listener.Addr())
@@ -179,6 +181,22 @@ func (s *Server) Start(stage adapter.StartStage) error {
 	return nil
 }
 
+func classifyControllerListenError(address string, err error) error {
+	if !errors.Is(err, syscall.EADDRINUSE) {
+		return err
+	}
+	_, portText, splitErr := net.SplitHostPort(address)
+	port, portErr := strconv.ParseUint(portText, 10, 16)
+	if splitErr != nil || portErr != nil || port == 0 {
+		return err
+	}
+	return adapter.NewEndpointConflictError(
+		adapter.EndpointConflictController,
+		uint16(port),
+		err,
+	)
+}
+
 func (s *Server) Close() error {
 	return common.Close(
 		common.PtrOrNil(s.httpServer),
diff --git a/experimental/libbox/endpoint_conflict_test.go b/experimental/libbox/endpoint_conflict_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..34e4602ab060d563349c153be9cb942e6d6518c7
--- /dev/null
+++ b/experimental/libbox/endpoint_conflict_test.go
@@ -0,0 +1,41 @@
+package libbox
+
+import (
+	"errors"
+	"fmt"
+	"syscall"
+	"testing"
+
+	"github.com/sagernet/sing-box/adapter"
+	E "github.com/sagernet/sing/common/exceptions"
+)
+
+func TestRuntimeStartConflictPreservesExactRoleAndPort(t *testing.T) {
+	err := fmt.Errorf(
+		"start service: %w",
+		adapter.NewEndpointConflictError(adapter.EndpointConflictController, 9092, syscall.EADDRINUSE),
+	)
+	conflict := runtimeStartConflict(err)
+	if conflict == nil || conflict.Kind != RuntimeEndpointConflictController || conflict.Port != 9092 {
+		t.Fatalf("unexpected runtime conflict: %#v", conflict)
+	}
+}
+
+func TestRuntimeStartConflictRejectsOrdinaryAndMalformedErrors(t *testing.T) {
+	for _, err := range []error{
+		syscall.EADDRINUSE,
+		errors.New("ordinary startup failure"),
+		adapter.NewEndpointConflictError(adapter.EndpointConflictMixed, 7890, errors.New("wrong cause")),
+		adapter.NewEndpointConflictError(adapter.EndpointConflictMixed, 7890, nil),
+		adapter.NewEndpointConflictError(adapter.EndpointConflictNone, 9090, syscall.EADDRINUSE),
+		adapter.NewEndpointConflictError(adapter.EndpointConflictMixed, 0, syscall.EADDRINUSE),
+		E.Errors(
+			adapter.NewEndpointConflictError(adapter.EndpointConflictMixed, 7890, syscall.EADDRINUSE),
+			errors.New("cleanup failed"),
+		),
+	} {
+		if conflict := runtimeStartConflict(err); conflict != nil {
+			t.Fatalf("unexpected retryable conflict for %v: %#v", err, conflict)
+		}
+	}
+}
diff --git a/protocol/mixed/endpoint_conflict_test.go b/protocol/mixed/endpoint_conflict_test.go
new file mode 100644
index 0000000000000000000000000000000000000000..f74d12c5aeeb267e9e7f4669ab3a5972f128c0d1
--- /dev/null
+++ b/protocol/mixed/endpoint_conflict_test.go
@@ -0,0 +1,35 @@
+package mixed
+
+import (
+	"errors"
+	"fmt"
+	"syscall"
+	"testing"
+
+	"github.com/sagernet/sing-box/adapter"
+)
+
+func TestClassifyMixedListenError(t *testing.T) {
+	cause := fmt.Errorf("listen failed: %w", syscall.EADDRINUSE)
+	err := classifyMixedListenError(7892, cause)
+	role, port, ok := adapter.EndpointConflictFromError(err)
+	if !ok || role != adapter.EndpointConflictMixed || port != 7892 || !errors.Is(err, cause) {
+		t.Fatalf("unexpected mixed conflict: role=%v port=%d ok=%v err=%v", role, port, ok, err)
+	}
+}
+
+func TestClassifyMixedListenErrorRejectsOtherAndMalformedErrors(t *testing.T) {
+	ordinary := errors.New("permission denied")
+	for _, test := range []struct {
+		port uint16
+		err  error
+	}{
+		{7892, ordinary},
+		{0, syscall.EADDRINUSE},
+	} {
+		classified := classifyMixedListenError(test.port, test.err)
+		if classified != test.err {
+			t.Fatalf("unexpected classification for port %d: %v", test.port, classified)
+		}
+	}
+}
diff --git a/protocol/mixed/inbound.go b/protocol/mixed/inbound.go
index c6cd08cb6dc8c421c3c66fe1e3267820a8825768..65542e3982989f3cc62c91ea2f3308696dcb70dd 100644
--- a/protocol/mixed/inbound.go
+++ b/protocol/mixed/inbound.go
@@ -3,7 +3,9 @@ package mixed
 import (
 	std_bufio "bufio"
 	"context"
+	"errors"
 	"net"
+	"syscall"
 	"time"
 
 	"github.com/sagernet/sing-box/adapter"
@@ -89,7 +91,17 @@ func (h *Inbound) Start(stage adapter.StartStage) error {
 			return E.Cause(err, "create TLS config")
 		}
 	}
-	return h.listener.Start()
+	return classifyMixedListenError(
+		h.listener.ListenOptions().ListenPort,
+		h.listener.Start(),
+	)
+}
+
+func classifyMixedListenError(port uint16, err error) error {
+	if !errors.Is(err, syscall.EADDRINUSE) || port == 0 {
+		return err
+	}
+	return adapter.NewEndpointConflictError(adapter.EndpointConflictMixed, port, err)
 }
 
 func (h *Inbound) Close() error {
